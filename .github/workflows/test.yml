name: Tests

on:
  - push
  - pull_request

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: test
    steps:
      - uses: actions/checkout@v3
      - uses: nanasess/setup-php@v3
        with:
          php-version: '5.6'
      - run: php test.php